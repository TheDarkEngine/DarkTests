#ifndef DARKTESTS_MAIN_HPP
#define DARKTESTS_MAIN_HPP

#include "Platform.hpp"

void ShowPlayernameLoop();
void SetPlayerName();
void gen_random(char *s, const int len);

std::string GetLastErrorAsString();
#endif /* DARKTESTS_MAIN_HPP */
